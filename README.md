* First, run the script.py to crawl the historical workshops (manual URL switching is required)
* Then run the script2.py to crawl the pages other than workshops (this program can also crawl the workshops, but due to the different naming rules of the folders we crawled earlier, this approach was retained to maintain the differences)
* Based on the file folder format of the downloaded data files, conduct experimental analysis (data cleaning and result visualization), and run the following programs respectively: deal_data.py (data cleaning), analyze_data.py (initial analysis), IGF_LLM_DeepExtractor.py (LLM semantic extraction)
* Other scripts can be executed after the above steps are completed and are used to generate other results.
* ....
